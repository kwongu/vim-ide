# vim-plug 가 클론해 오는 플러그인은 추적하지 않는다.
# (이미 추적 중인 것들은 그대로 남는다 - .gitignore 는 추적 파일에
#  영향을 주지 않는다. 새로 설치한 플러그인이 git add -A 에 딸려
#  들어가는 것만 막는다.)
.vim/plugged/

# nvim 이 저장소 안에 남기는 실행 상태(로그 등). 설정이 아니다.
# .local/bin 의 도우미 스크립트는 그대로 추적한다.
.local/state/
